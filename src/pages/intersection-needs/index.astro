---
import {apiGet} from '../../script/util.js';
import IdLabel from '../../components/IdLabel.astro';

const section = "intersection-needs"
const json = await apiGet(section);

// eliminate duplicates
let seen = new Array();
let i = 0;
json.forEach(function(item) {
	let id = item.id;
	/*
	if (typeof findObjectByProperties(seen, {"id": id}) !== 'undefined') {
		json.splice(i, 1);
	} else {
		seen.push(id);
	}
	*/
	i++;
});

---

<IdLabel section="intersection-needs"></IdLabelSupports>

