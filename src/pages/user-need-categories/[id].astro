---
import MatrixItemDetails from '../../components/MatrixItemDetails.astro';

export async function getStaticPaths() { 
	const module = await import('./ids.js');
	const {ids} = module;
	return (ids);
}

const {id} = Astro.params;
---
<MatrixItemDetails id={id} section="user-need-categories" type="User Need Category"></MatrixItemDetails>

