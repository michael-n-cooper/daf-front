---
import Matrix from '../../layouts/Matrix.astro';
import { buildMatrix } from './build-matrix';

let table = await buildMatrix();
---
<Matrix title="Accommodation types matrix" table={table}></Matrix>
